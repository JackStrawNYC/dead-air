/**
 * RainStreetScene — wet city street at night, noir cinematic atmosphere.
 * Rain streaks, puddle reflections, neon light, ground fog.
 */

import React from "react";
import { AudioReactiveCanvas } from "../components/AudioReactiveCanvas";
import { FullscreenQuad } from "../components/FullscreenQuad";
import { rainStreetVert, rainStreetFrag } from "../shaders/rain-street";
import type { EnhancedFrameData, SectionBoundary, ColorPalette } from "../data/types";

interface Props {
  frames: EnhancedFrameData[];
  sections?: SectionBoundary[];
  palette?: ColorPalette;
  tempo?: number;
  style?: React.CSSProperties;
  jamDensity?: number;
}

export const RainStreetScene: React.FC<Props> = ({ frames, sections, palette, tempo, style, jamDensity }) => {
  return (
    <AudioReactiveCanvas frames={frames} sections={sections} palette={palette} tempo={tempo} style={style} jamDensity={jamDensity}>
      <FullscreenQuad
        vertexShader={rainStreetVert}
        fragmentShader={rainStreetFrag}
      />
    </AudioReactiveCanvas>
  );
};
